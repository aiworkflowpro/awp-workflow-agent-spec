{
  "schema_version": "1.0",
  "skill": "example-workflow-skill",
  "profiles": ["minimal", "standard"],
  "envs": [
    {
      "id": "shell-main",
      "kind": "bash",
      "required": true,
      "version": ">=3.2",
      "install_hint": "Use the OS-provided bash, or install via your system package manager."
    }
  ],
  "binaries": [
    {
      "name": "rg",
      "required": false,
      "purpose": "Fast text scanning. Falls back to grep when missing."
    }
  ],
  "models": [],
  "cache_policy": {
    "root": "runtime",
    "sync": false,
    "allow_provider_cache": false
  },
  "network": {
    "required": false,
    "hosts": [],
    "offline_behavior": "stop-or-degrade-with-record"
  }
}
